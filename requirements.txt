# Core
numpy>=1.24
scipy>=1.10
pandas>=2.0
pyyaml>=6.0
tqdm>=4.66
rich>=13.7

# PyTorch (install CUDA/CPU build as appropriate for your machine)
torch>=2.2
torchvision>=0.17

# Config / experiment management
hydra-core>=1.3
omegaconf>=2.3

# Modeling utils
einops>=0.7

# Geometry / manifolds (for hyperbolic + Riemannian ops)
geoopt>=0.5.0
geomstats>=2.6.0

# Logging / visualization
matplotlib>=3.8
tensorboard>=2.15

# Optional (uncomment if you want)
# wandb>=0.16

